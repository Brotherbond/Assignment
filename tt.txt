Scree plot is a plot which can be used to get the elbow point which is used to break the dataset for optimal classification. The K-means is determined from scree plotx,NB: K-means is an integer and is selected as the point where further changes down the graph is almost linear. 